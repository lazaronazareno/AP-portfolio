import { MissingImgDirective } from './missing-img.directive';

describe('MissingImgDirective', () => {
  it('should create an instance', () => {
    const directive = new MissingImgDirective();
    expect(directive).toBeTruthy();
  });
});
